public class NameConventions {
     /**
      * 
        Class name = Capital letter in every word - Pascal casing
        variable = caMel Casing.

        Always contain only one main method
        
        string in quotation marks = string literal and have null as default.

        public = to be access from other areas, classes.
        private = variables will be only access by same class methods

        methods = acess modifier + Static(or not) + Return type

        Constructor = If a class does not define constructors, the compiler provides a
          default constructor with no parameters. 
          But if you declare, default is not created.

      */



}